
syntax = "proto3";

package twitteranomalydetection;


service TwitterAnomalyDetection {

  rpc DetectAnomalies (Request) returns (Response) {}
}

message Request {
  string raw_json = 1;
  string only_last = 2;
  bool resampling = 3; 

}

message Response {
  repeated float entries = 1; 
}
  
